/**
 * Returns a markup template string for a not found page.
 * @returns {string} A markup template string for a not found page.
*/
function NotFoundTemplate() {
    return `
        <div class="placeholder-block">
            <p>404 | NOT FOUND</p>
        </div>
    `
}